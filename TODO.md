# TODO

## Features

- [x] Preview the uploaded profile photo on the "Documents" step of the join wizard (`join_documents.php`) — currently the file input only shows the filename (e.g. "images.jpeg"), no thumbnail of the actual photo.
- [x] Show an itemized breakdown (cotisation/subscription, licence, group lessons, credits, discount, promocode) in the payment receipt and in the SumUp checkout — for admins reviewing payments, not just a single total amount. Done for the receipt (`payment_result.php`) and a new admin order-detail page (`/admin/commandes/{id}`), both via a shared `OrderBreakdownService` decoding `orders.cart_lines`/`promo_code_id`. SumUp's own hosted checkout page can't show line items (confirmed against their API docs — single `amount` + `description` only, no products field); an embedded Payment Widget would solve that but is a separate, bigger integration change, left for later. Confirmation-email and invoicing itemization also deferred — `OrderBreakdownService` is built reusable for when that happens.
- [x] On the admin "Commandes" list (admin_orders.php), add a link per order to the corresponding member's profile in Balle Jaune — note join orders carry bj_user_id=0 until fulfillment, so the link may only be available for renewal/credits orders or once a join order is fulfilled. Done for both the list and the order-detail page (`/admin/commandes/{id}`); confirmed join orders never get `orders.bj_user_id` populated even after fulfillment (fulfillment writes each person's BJ id onto `application_people`, not back onto the order), so the link only ever shows for renewal/credits orders — the "once fulfilled" case never actually applies.
- [x] Let admins generate promo codes (e.g. a discount or fee waiver applied at checkout) to handle one-off pricing edge cases in the join/renewal flow, instead of needing a manual workaround or code change per case. Licences are never discounted (same rule as the existing prorata discount) — a percent code applies only to cotisation + lessons, and a fixed-€ code clamps to that narrower subtotal so it can't eat into licence money.
- [x] Add a "Garennois" badge next to the member name in admin list views (admin_members.php and related lists) when residence === 'garennois', so resident tarif status is visible at a glance without opening the record. Done broader than originally scoped, per follow-up: badge added to all 8 admin views displaying members (members search, licences/shoes queues, change requests, renewal campaign, applications ×3, and admin_orders/order-detail next to the email), plus Garennois-first sorting on the actionable queues (licences, shoes, change requests, campaign, applications). Verified live against real production BJ data (47 real Garennois residents correctly sorted to the top of the licences board).
- [ ] Add a dedicated "Paiements en attente" admin list for applications validated but not yet paid — query applications with status IN ('validated','awaiting_payment') (AdminApplicationController, new route alongside /admin/demandes and /admin/demandes/abandonnees). Currently a validated application is invisible in the admin UI until the applicant reaches checkout and an order row exists (visible only via /admin/commandes with status 'En attente').
- [x] Restyle the "Représentant légal" step's guardian-contact field (renewal_guardian.php, RenewalController::submitGuardian(), single `guardian_contact` textarea stored as BJ's `custom1`) as a proper structured form (e.g. separate name/email/phone fields) instead of one free-text box — matching the structured guardian_fullname/guardian_email/guardian_phone fields already used in the join wizard's application_people table. Guardian's contact detail. Done: extracted the "fullname · email · phone" encode/decode into a shared `App\Service\GuardianContact` (format()/parse()), used by both `FulfillmentService` (join wizard, write-only) and `RenewalController` (renewal step, both directions so it can prefill). Kept the step optional — no `required` attributes, only validates email format when non-empty — and parse() degrades gracefully for legacy free-text values that predate this format (the whole string lands in the fullname field rather than being lost). Verified live: round-trips correctly through BJ's custom1, and a real legacy comma-separated value on a test member correctly fell back to the fullname-only case.
- [x] Add a "Marquer comme remboursé" admin action per order (admin_orders.php list + the new /admin/commandes/{id} detail page) to record that a payment was refunded outside the app (e.g. via SumUp's dashboard after a mistake like an accidental renewal) — likely a new `refunded_at` timestamp column on `orders`, kept separate from the existing `status` enum so the original paid/fulfilled status stays visible, surfaced as a badge in both the list and detail view.
- [x] Add an admin-facing page to browse the audit_log table (actor, action, entity, entity_id, details, created_at) — entries are already written for licence/shoes/order/promo-code/application actions via each controller's audit() helper, but there's no UI to view them.

## Bugs

- [x] On the renewal formule step (renewal_form.php), the "Inscription en couple" checkbox and "Conjoint(e)" fields are always shown, even for members with no indication they're part of a couple — should only appear when the member's current subscription/BJ data actually shows a couple status.
- [x] On the renewal "Formule" step (renewal_form.php), when a late renewal falls into the Pack été window, the page still shows the normal subscription radio buttons (Heures Pleines/Heures Creuses, at full price) instead of presenting "Pack été" itself as the offered formula — the Pack été framing only appears later, on the payment/cart step (renewal_cart.php).
- [x] During Pack été (renewal_form.php Formule step, and the equivalent join summer-pack flow), only "Heures Pleines" should be offered as a formula — currently all three options (Heures Pleines, Heures Creuses, *and* Midi) are shown as selectable, each labeled "(Pack été — tarif unique, voir ci-dessus)". Confirmed via E2E on the renewal flow (throwaway test member, choice=ete path). Done: `RenewalController::context()` now filters `$context['subscriptions']` down to `heures-pleines` only when `$lateSettlement` is true, so Formule only ever offers that one option and `submit()`'s existing validation naturally rejects anything else — no template special-casing needed. The join flow needed no change: `join_formule.php` already shows fixed "Heures Pleines" text (not a selectable loop) during `$isSummerPack`. Verified live: Pack-été test member sees only Heures Pleines and submits straight through to Licence with no unwanted admin-approval detour.
- [x] ~~On the join wizard's Formule step (/inscription), a new member joining during the Pack été window is only ever offered the flat Pack été price... no option to join directly for the upcoming season... the join flow appears to have no equivalent [to the renewal choice screen]~~ — **correction after investigation**: this diagnosis was wrong. `ProspectController::submitStart()` (lines 93-121) already has the exact equivalent — a "Quelle formule choisir ?" modal (`join_start.php:21-34`) offering Pack été vs. the next published season, verified working end-to-end via E2E (both branches produce the correct outcome: Pack été framing vs. a clean next-season application with no Pack été text at all). The only real bug was the copy: `join_formule.php:16` said "Vous pourrez **renouveler** normalement à la rentrée" (renewal-flavored wording) to a first-time joiner who has nothing to renew — fixed to "Vous pourrez **adhérer** au tarif plein à la rentrée." The other two "renouvellement" mentions in the join templates (`join_start.php`/`join_informations.php`) are correctly telling an *existing* member to use renewal instead, not a bug.
- [ ] In RenewalController::context() (RenewalController.php:461), the pending/approved change-request lookup (pendingChangeRequest) runs with $season->startYear BEFORE the choice-based season advancement at lines 496-498 (the "jump to next season" branch, $season = $season->next()). For a member who picked 'next season' on the Pack été choice screen and then submitted a formula change, the change_requests row is correctly saved under the advanced season — but every later visit to /espace/renouvellement looks up the request under the WRONG (pre-advance) season year and finds nothing. The member never sees 'en attente de validation' or the auto-applied approved change; they just see the formule form again and can resubmit indefinitely. Worse than a display gap: since submit() also gates on context()['redirect'] === null before accepting a new submission, this same lookup bug lets a member submit a SECOND, different renewal choice while a first request is still genuinely pending — confirmed live (a member with a pending couple-status change request successfully submitted a plain individual renewal right after and was sent straight to payment, no block, no error). Fix: move the pendingChangeRequest() call (and the redirect/approved-branch logic that depends on it) to after the choice-based $season reassignment. Found via manual E2E: created throwaway BJ test accounts, picked 'next season', submitted a formula change (Heures Pleines -> Heures Creuses), had the admin approve it, then reloaded the member's /espace/renouvellement — got the plain formule form with the OLD subscription still marked 'abonnement actuel', not the expected change_approved status page.
- [x] Style the "Saison 2026-2027" button in the Pack été choice modal (join_start.php, ~line 30) — it had no CSS class and rendered as an unstyled default button next to the outlined "Pack été" button. Fixed as part of building the join-flow "reconsider Pack été" screen (which needed the same two-button pattern to be visually consistent) — added `class="btn"`.
- [ ] Show member name (LASTNAME, Firstname) instead of email as the primary identifier in admin list views — audit admin_members.php, admin_applications_abandoned.php, admin_orders.php, admin_change_requests.php, admin_lessons.php, admin_shoes.php and admin_campaign.php for tables/lists currently labeling rows by email only.
- [x] Add a "Précédent" button on the renewal payment step (renewal_cart.php, step 2 "Paiement") to go back to the Formule step — currently there's no way back, only "Payer ... en ligne". Done: a `btn-outline` link to `/espace/renouvellement` next to the "Payer" button; that route safely re-renders whatever step context() currently resolves to (Formule, or the licence gate/status page if applicable) without losing the in-progress intent. Verified live: selected subscription stayed intact after navigating back and forth.
- [ ] Verify couple + Garennois renewal pricing for clem.georget@gmail.com (partner BJ user 1705231, Heures Pleines) — confirm whether the couple cotisation should total 240€ + 40€ licence (2×20€) and check PricingService/RenewalController compute this correctly for that case.
- [x] Couple renewal cotisation computes a garbage amount for Heures Pleines couples — stella.jovet@gmail.com + partner BJ user 2013741 (Hors commune) shows "Cotisation — Heures Pleines — Couple (renouvellement) 35,67 €" in the cart instead of the expected flat couple rate. Same suspected issue for the Garennois case (clem.georget@gmail.com + partner 1705231, see prior TODO item). Check the couple cotisation branch in PricingService/RenewalService for renewals.
- [ ] Pack été (late-settlement) is currently offered to Jeune subscribers, but doesn't make sense for them (their subscription already has its own separate, lower pricing/licence structure) — same as couples, which are already explicitly excluded from Pack été in RenewalController::context() (the `$lateSettlement && $couple['isCouple']` block: skip to next season if published, else 'awaiting next season' message). Jeune members hitting late settlement should get that same treatment instead of being routed into Pack été. Check both the renewal flow (RenewalController) and the equivalent join summer-pack flow (ProspectController).
- [x] On /admin/campagne (AdminRenewalController::campaign()), the member list should only include members who do NOT have a valid subscription for the current season or the next one — currently it doesn't filter (or doesn't filter correctly) on that, showing members who don't need the renewal campaign. Done: `yearlyMembers()` now excludes anyone whose `RenewalService::renewalTarget()` state isn't `'open'` (already covered through next season, or nothing to renew into yet), instead of a naive "not yet expired" check. Verified live and independently re-derived against real BJ data: 373 campaign-eligible out of 382 real members, the excluded 9 all already renewed through 2026-2027.
