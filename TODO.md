# TODO

## Features

- [x] Preview the uploaded profile photo on the "Documents" step of the join wizard (`join_documents.php`) — currently the file input only shows the filename (e.g. "images.jpeg"), no thumbnail of the actual photo.
- [x] Show an itemized breakdown (cotisation/subscription, licence, group lessons, credits, discount, promocode) in the payment receipt and in the SumUp checkout — for admins reviewing payments, not just a single total amount. Done for the receipt (`payment_result.php`) and a new admin order-detail page (`/admin/commandes/{id}`), both via a shared `OrderBreakdownService` decoding `orders.cart_lines`/`promo_code_id`. SumUp's own hosted checkout page can't show line items (confirmed against their API docs — single `amount` + `description` only, no products field); an embedded Payment Widget would solve that but is a separate, bigger integration change, left for later. Confirmation-email and invoicing itemization also deferred — `OrderBreakdownService` is built reusable for when that happens.
- [x] On the admin "Commandes" list (admin_orders.php), add a link per order to the corresponding member's profile in Balle Jaune — note join orders carry bj_user_id=0 until fulfillment, so the link may only be available for renewal/credits orders or once a join order is fulfilled. Done for both the list and the order-detail page (`/admin/commandes/{id}`); confirmed join orders never get `orders.bj_user_id` populated even after fulfillment (fulfillment writes each person's BJ id onto `application_people`, not back onto the order), so the link only ever shows for renewal/credits orders — the "once fulfilled" case never actually applies.
- [x] Let admins generate promo codes (e.g. a discount or fee waiver applied at checkout) to handle one-off pricing edge cases in the join/renewal flow, instead of needing a manual workaround or code change per case. Licences are never discounted (same rule as the existing prorata discount) — a percent code applies only to cotisation + lessons, and a fixed-€ code clamps to that narrower subtotal so it can't eat into licence money.
- [x] Add a "Garennois" badge next to the member name in admin list views (admin_members.php and related lists) when residence === 'garennois', so resident tarif status is visible at a glance without opening the record. Done broader than originally scoped, per follow-up: badge added to all 8 admin views displaying members (members search, licences/shoes queues, change requests, renewal campaign, applications ×3, and admin_orders/order-detail next to the email), plus Garennois-first sorting on the actionable queues (licences, shoes, change requests, campaign, applications). Verified live against real production BJ data (47 real Garennois residents correctly sorted to the top of the licences board).
- [ ] Add a dedicated "Paiements en attente" admin list for applications validated but not yet paid — query applications with status IN ('validated','awaiting_payment') (AdminApplicationController, new route alongside /admin/demandes and /admin/demandes/abandonnees). Currently a validated application is invisible in the admin UI until the applicant reaches checkout and an order row exists (visible only via /admin/commandes with status 'En attente').
- [x] Restyle the "Représentant légal" step's guardian-contact field (renewal_guardian.php, RenewalController::submitGuardian(), single `guardian_contact` textarea stored as BJ's `custom1`) as a proper structured form (e.g. separate name/email/phone fields) instead of one free-text box — matching the structured guardian_fullname/guardian_email/guardian_phone fields already used in the join wizard's application_people table. Guardian's contact detail. Done: extracted the "fullname · email · phone" encode/decode into a shared `App\Service\GuardianContact` (format()/parse()), used by both `FulfillmentService` (join wizard, write-only) and `RenewalController` (renewal step, both directions so it can prefill). Kept the step optional — no `required` attributes, only validates email format when non-empty — and parse() degrades gracefully for legacy free-text values that predate this format (the whole string lands in the fullname field rather than being lost). Verified live: round-trips correctly through BJ's custom1, and a real legacy comma-separated value on a test member correctly fell back to the fullname-only case.
- [x] Add a "Marquer comme remboursé" admin action per order (admin_orders.php list + the new /admin/commandes/{id} detail page) to record that a payment was refunded outside the app (e.g. via SumUp's dashboard after a mistake like an accidental renewal) — likely a new `refunded_at` timestamp column on `orders`, kept separate from the existing `status` enum so the original paid/fulfilled status stays visible, surfaced as a badge in both the list and detail view.
- [x] Add an admin-facing page to browse the audit_log table (actor, action, entity, entity_id, details, created_at) — entries are already written for licence/shoes/order/promo-code/application actions via each controller's audit() helper, but there's no UI to view them.
- [x] Require admin approval before payment for any order using a promo code — previously a code applied automatically straight through to SumUp checkout with no human in the loop beyond whoever created the code. Done: a new `orders.status` value (`awaiting_promo_approval`) sits before `pending` — `startCheckout()` (join and renewal) creates the order with the discount already frozen in but skips creating a SumUp checkout entirely, redirecting instead to the existing shared `/paiement/retour/{reference}` → `payment_result.php` status page (new branch there). A new admin queue at `/admin/codes-promo/approbations` (in `AdminPromoCodeController`, alongside the create/edit/archive actions from the previous promo-code TODO) lets an admin approve — which only then creates the real SumUp checkout and emails the payment link — or refuse, which cancels the order and lets the member continue at full price (join: `applications.promo_code` is cleared directly; renewal's session-only intent instead relies on a `hasRefusedPromoUsage()` check in `startCheckout()` so re-entering the same refused code doesn't spawn a second pending request). Verified live end-to-end for both join and renewal: apply → pending-approval page (no SumUp redirect) → admin approves → real checkout created + email sent with working payment link; separately, apply → admin refuses → code cleared/dropped → member completes checkout at full price with no duplicate request created on retry.
- [x] Make phone numbers clickable to open a WhatsApp chat with that person, for admins to message applicants/guardians directly instead of retyping digits. Audit found only three actual phone-number displays in the whole app (none of them the club's own number, which doesn't appear anywhere) — applicant and guardian phone on `admin_application_detail.php`, and applicant phone on `admin_applications_abandoned.php` — all admin-facing. Done: new `App\Support\WhatsApp::link()` normalizes the stored digits (French local `0X XX XX XX XX` → `33X XX XX XX XX`; already-international and odd shapes pass through best-effort, since stored numbers were never format-validated at entry — real data mixes both shapes) into a `https://wa.me/...` link, wrapping the existing plain-text phone display at each of the three call sites; falls back to plain text only when there are no digits at all (an incomplete abandoned draft). Verified: new `WhatsAppTest` unit tests, plus live against real application data — both applicant and guardian links resolve to the correctly normalized `wa.me` target on the detail page, and every populated row on the abandoned-drafts list renders as a working link.
- [ ] Build a credit-note (avoir) flow for the invoicing module: when AdminOpsController marks a fulfilled join/renewal order as refunded/canceled/processed, the already-issued invoice (invoices table) currently stays immutable with no credit note referencing the original invoice number — proper French accounting practice wants one.

## Bugs

- [x] On the renewal formule step (renewal_form.php), the "Inscription en couple" checkbox and "Conjoint(e)" fields are always shown, even for members with no indication they're part of a couple — should only appear when the member's current subscription/BJ data actually shows a couple status.
- [x] On the renewal "Formule" step (renewal_form.php), when a late renewal falls into the Pack été window, the page still shows the normal subscription radio buttons (Heures Pleines/Heures Creuses, at full price) instead of presenting "Pack été" itself as the offered formula — the Pack été framing only appears later, on the payment/cart step (renewal_cart.php).
- [x] During Pack été (renewal_form.php Formule step, and the equivalent join summer-pack flow), only "Heures Pleines" should be offered as a formula — currently all three options (Heures Pleines, Heures Creuses, *and* Midi) are shown as selectable, each labeled "(Pack été — tarif unique, voir ci-dessus)". Confirmed via E2E on the renewal flow (throwaway test member, choice=ete path). Done: `RenewalController::context()` now filters `$context['subscriptions']` down to `heures-pleines` only when `$lateSettlement` is true, so Formule only ever offers that one option and `submit()`'s existing validation naturally rejects anything else — no template special-casing needed. The join flow needed no change: `join_formule.php` already shows fixed "Heures Pleines" text (not a selectable loop) during `$isSummerPack`. Verified live: Pack-été test member sees only Heures Pleines and submits straight through to Licence with no unwanted admin-approval detour.
- [x] ~~On the join wizard's Formule step (/inscription), a new member joining during the Pack été window is only ever offered the flat Pack été price... no option to join directly for the upcoming season... the join flow appears to have no equivalent [to the renewal choice screen]~~ — **correction after investigation**: this diagnosis was wrong. `ProspectController::submitStart()` (lines 93-121) already has the exact equivalent — a "Quelle formule choisir ?" modal (`join_start.php:21-34`) offering Pack été vs. the next published season, verified working end-to-end via E2E (both branches produce the correct outcome: Pack été framing vs. a clean next-season application with no Pack été text at all). The only real bug was the copy: `join_formule.php:16` said "Vous pourrez **renouveler** normalement à la rentrée" (renewal-flavored wording) to a first-time joiner who has nothing to renew — fixed to "Vous pourrez **adhérer** au tarif plein à la rentrée." The other two "renouvellement" mentions in the join templates (`join_start.php`/`join_informations.php`) are correctly telling an *existing* member to use renewal instead, not a bug.
- [x] Style the "Saison 2026-2027" button in the Pack été choice modal (join_start.php, ~line 30) — it had no CSS class and rendered as an unstyled default button next to the outlined "Pack été" button. Fixed as part of building the join-flow "reconsider Pack été" screen (which needed the same two-button pattern to be visually consistent) — added `class="btn"`.
- [x] Show member name (LASTNAME, Firstname) instead of email as the primary identifier in admin list views — audit admin_members.php, admin_applications_abandoned.php, admin_orders.php, admin_change_requests.php, admin_lessons.php, admin_shoes.php and admin_campaign.php for tables/lists currently labeling rows by email only. Audit result: 6 of 7 already showed name as the primary identifier (a side effect of the earlier Garennois-badge work, which per its own note went "broader than originally scoped"). Only `admin_orders.php` genuinely was email-only — fixed, along with its companion `admin_order_detail.php` which had the same gap. Added a "Nom" column resolved two ways depending on order kind: join orders (bj_user_id always 0) via a new `LEFT JOIN application_people` in the list query / a direct query in `AdminOpsController::nameForOrder()` for the detail page; renewal/credits/change orders via the existing batched BJ lookup in `withOrderResidence()` (renamed `withOrderResidenceAndName()`) — extended to also capture firstname/lastname from the same response, no extra API calls. Verified live against real order data spanning both kinds and 18 archived orders, including one row with a since-deleted BJ user correctly falling back to "—" instead of erroring.
- [x] Add a "Précédent" button on the renewal payment step (renewal_cart.php, step 2 "Paiement") to go back to the Formule step — currently there's no way back, only "Payer ... en ligne". Done: a `btn-outline` link to `/espace/renouvellement` next to the "Payer" button; that route safely re-renders whatever step context() currently resolves to (Formule, or the licence gate/status page if applicable) without losing the in-progress intent. Verified live: selected subscription stayed intact after navigating back and forth.
- [x] Verify couple + Garennois renewal pricing for clem.georget@gmail.com (partner BJ user 1705231, Heures Pleines) — confirm whether the couple cotisation should total 240€ + 40€ licence (2×20€) and check PricingService/RenewalController compute this correctly for that case.
- [x] Couple renewal cotisation computes a garbage amount for Heures Pleines couples — stella.jovet@gmail.com + partner BJ user 2013741 (Hors commune) shows "Cotisation — Heures Pleines — Couple (renouvellement) 35,67 €" in the cart instead of the expected flat couple rate. Same suspected issue for the Garennois case (clem.georget@gmail.com + partner 1705231, see prior TODO item). Check the couple cotisation branch in PricingService/RenewalService for renewals.
- [x] Pack été (late-settlement) is currently offered to Jeune subscribers, but doesn't make sense for them (their subscription already has its own separate, lower pricing/licence structure) — same as couples, which are already explicitly excluded from Pack été in RenewalController::context() (the `$lateSettlement && $couple['isCouple']` block: skip to next season if published, else 'awaiting next season' message). Jeune members hitting late settlement should get that same treatment instead of being routed into Pack été. Check both the renewal flow (RenewalController) and the equivalent join summer-pack flow (ProspectController). Done: added a sibling `$lateSettlement && $isJeune` exclusion block in RenewalController::context() (new `jeune_awaiting_next_season` status state + copy in renewal_status.php), plus a new private `isJeune(bjUser, season)` helper — needed because `PricingService::isJeune()` is season-dependent (age at Sept 1), so it has to be evaluated twice: once pre-advancement to gate the exclusion, once post-advancement for everything downstream, unlike the couple check which only needs one evaluation. This was more than cosmetic: after the TODO #2 fix (Pack été restricted to Heures Pleines only), a Jeune member hitting late settlement got an *empty, non-completable* Formule step (Jeune-audience ∩ heures-pleines-only = empty) — a real dead end, not just wrong pricing. ProspectController (join flow) needed no changes — verified already fully correct at every entry point (submitStart, the formule-saison reconsideration screen, submitFormule). Verified live against the real BJ test member (temporarily patched birthdate/subscription_date_end via a throwaway script, restored after): season-advance path showed a proper non-empty Jeune formula and completed through to Santé; wait-redirect path (pricing.2026-2027.php briefly moved aside) showed the new Jeune-worded status page, not the couple wording and not an empty form.
- [x] On /admin/campagne (AdminRenewalController::campaign()), the member list should only include members who do NOT have a valid subscription for the current season or the next one — currently it doesn't filter (or doesn't filter correctly) on that, showing members who don't need the renewal campaign. Done: `yearlyMembers()` now excludes anyone whose `RenewalService::renewalTarget()` state isn't `'open'` (already covered through next season, or nothing to renew into yet), instead of a naive "not yet expired" check. Verified live and independently re-derived against real BJ data: 373 campaign-eligible out of 382 real members, the excluded 9 all already renewed through 2026-2027.
- [x] SumUp's webhook delivery (`CHECKOUT_STATUS_CHANGED`) was unreachable in production: SumUp has no dashboard/API webhook-URL setting — it POSTs the event to the checkout's own `return_url`, the same URL the browser is redirected to (GET) after payment. The app only registered `GET /paiement/retour/{reference}`; the standalone `POST /webhooks/sumup` route existed but SumUp had no way to know about it, so it would never receive traffic. Payments would still complete via the browser-return path, but not if the customer closed the tab before the redirect landed. Found while setting up the real webhook for go-live (SumUp's own docs state the return_url mechanism explicitly). Fixed: registered `POST` on the same `/paiement/retour/{reference}` path (removed the dead `/webhooks/sumup` route) and updated `PaymentController::webhook()` to resolve the order from the URL's `{reference}` segment first, falling back to the JSON body's `checkout_reference`/`id` — SumUp's real payload only carries `id` (checkout id), not our reference, per their sample payload. Verified: unit tests pass, deployed to production, `/sante` still green post-deploy.
- [x] The bug-report bubble's screenshot (bug-report.js, html2canvas) captured the "Signaler un problème" modal itself instead of the underlying page the reporter was looking at — the modal is still open (and thus part of the DOM) at capture time, since the form doesn't close until the report succeeds. Reported live in production via a screenshot showing the modal baked into the capture. Fixed: pass html2canvas an `ignoreElements` callback excluding the modal node, so the render shows whatever is behind it instead. Verified locally by running the exact capture call in-browser with and without the fix and sampling a pixel at a button's known location behind the modal — without the fix: pure white (the modal's background); with the fix: the button's actual green — plus a full visual capture confirming no trace of the modal.
- [x] No resume path for a plain 'pending' renewal order that already has a SumUp checkout attached (app/src/Controller/RenewalController.php, app/src/Repository/OrderRepository.php) — same gap found in join/credits/lessons too, plus a deeper one: fulfillment had no idempotency above a single order row, so if a duplicate order ever also got marked paid (e.g. a hosted-checkout page erroring after the underlying charge actually succeeded, then a delayed webhook), fulfillJoin()/fulfillRenewal() would run a second time — creating a second BJ account or issuing a second invoice — since nothing checked "is this application/member-season already fulfilled by another order". Done: new `App\Service\PaymentSettlementService` (settle() moved out of PaymentController; new resumeIfOpen()) is called from startCheckout() in PaymentController (join), RenewalController, CreditsController and LessonSignupController before creating a new order — it checks the existing open order's real status with SumUp and either resumes its checkout, settles it in place if it turns out already paid, or closes it out as failed so a fresh one is safe to create (including on a SumUp lookup error, so a stale/expired checkout can't trap a member). Separately, FulfillmentService::fulfillJoin()/fulfillRenewal() now guard against double-fulfillment via a purely local check (application.status for join; a new RenewalService::hasFulfilledFormula() reading member_formulas for renewal — deliberately never BJ's own date, so a manual BJ correction can't wrongly suppress a genuine renewal) — a caught duplicate is still marked 'fulfilled' (money was genuinely taken) but flagged `meta.duplicateFulfillment` for admin review/refund, surfaced as a banner on the order detail page and a badge on the orders list. New tests: RenewalServiceTest (hasFulfilledFormula) and PaymentSettlementServiceTest; full suite green (131 tests). Not yet deployed to production as of 2026-08-28.
- [x] Magic-link login completes on a bare GET (AuthController::verify(), app/src/Controller/AuthController.php:106-143 calling AuthService::consumeToken(), app/src/Service/Auth/AuthService.php:89-107), so corporate email security scanners (Safe Links, Proofpoint, Mimecast) that auto-prefetch links silently burn the single-use token before the real member clicks it — confirmed on prod 2026-08-28 via LEBIAN/jeanlouis.lebian@coface.com (3 links sent+consumed server-side with clean [auth] Login log entries, but he never got in, matching a corporate scanner pre-visiting the URL). Fix: split verify() into a GET landing page that only validates the token format (no DB consume) plus a CSRF-protected POST that actually calls consumeToken() and logs in, triggered only by a confirm-button click — scanners fetch GET but don't submit forms. Keep the 15-min TTL and single-use semantics unchanged; also check the ambiguous bj_user_id=0 branch (AuthController.php:117-129), which currently consumes on GET too. Test that a bot-style GET no longer burns the token and a real GET-then-POST click-through still logs in within 15 minutes, single-use.
